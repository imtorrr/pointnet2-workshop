name: Deploy to GCS

on:
  push:
    branches:
      - main
    paths:
      - 'index.html'
      - 'assets/**'

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCS_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Deploy to GCS
        run: |
          gsutil -m cp index.html gs://pointnet2-workshop/
          gsutil -m rsync -r -d assets/ gs://pointnet2-workshop/assets/
